#pragma once

// MailApplicationService was retired by structural refactor phase 5.
// Daemon application responsibilities are split across AccountRuntimeManager,
// MailQueryApplicationService, MailMutationApplicationService,
// MessageContentApplicationService, MailNotificationService, and the
// contact/calendar/Sieve application services.
